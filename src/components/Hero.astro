---
import { siteConfig } from "../config";
---

<div
  id="hero"
  class="relative isolate overflow-hidden bg-white py-24 md:h-screen"
  style={`--accent-color: ${siteConfig.accentColor}`}
>
  <!-- Gradient overlay -->
  <div
    class="absolute inset-0 -z-10"
    style={`background: radial-gradient(ellipse 800px 1200px at 0% 0%, ${siteConfig.accentColor}40 0%, ${siteConfig.accentColor}25 20%, ${siteConfig.accentColor}10 40%, rgba(255, 255, 255, 0.3) 70%, rgba(255, 255, 255, 0.8) 90%, white 100%)`}
  >
  </div>
  <svg
    aria-hidden="true"
    class="absolute inset-0 -z-10 size-full stroke-gray-200 [mask-image:radial-gradient(100%_100%_at_top_right,white,transparent)]"
  >
    <defs>
      <pattern
        x="50%"
        y={-1}
        id="0787a7c5-978c-4f66-83c7-11c213f99cb7"
        width={200}
        height={200}
        patternUnits="userSpaceOnUse"
      >
        <path d="M.5 200V.5H200" fill="none"></path>
      </pattern>
    </defs>
    <rect
      fill="url(#0787a7c5-978c-4f66-83c7-11c213f99cb7)"
      width="100%"
      height="100%"
      stroke-width={0}></rect>
  </svg>
  <div class="h-full mx-auto p-8 sm:p-12 md:p-24 flex items-center">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 w-full items-center">
      <!-- Left Section - Text Content -->
      <div class="space-y-6 sm:space-y-8 md:space-y-10">
        <h2
          class="text-pretty text-xl sm:text-2xl md:text-5xl font-bold tracking-tight text-gray-700 animate-fade-in"
        >
          Hello! 👋
        </h2>
        <h1
          class="text-pretty text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight text-gray-800 animate-fade-in animation-delay-200 whitespace-nowrap"
        >
          I'm <span style={{ color: siteConfig.accentColor }} class="whitespace-nowrap">
            {siteConfig.name}
          </span>
        </h1>
        
        <!-- Clean Professional Title -->
        <div class="animate-fade-in animation-delay-400">
          <div class="inline-flex items-center gap-3 px-4 py-2 bg-white/60 backdrop-blur-md rounded-lg border border-white/40 shadow-lg">
            <span class="text-lg sm:text-xl md:text-2xl font-bold text-gray-800">
              Java & DevOps Engineer
            </span>
          </div>
        </div>
      </div>

      <!-- Right Section - Floating Company Logos -->
      <div class="relative h-96 lg:h-full min-h-[400px] animate-fade-in animation-delay-600">
        <!-- Desktop: Floating Logo Container -->
        <div class="hidden lg:block absolute inset-0 overflow-hidden">
          
          <!-- Porsche Informatik - Bottom Center (Aligned with Fraunhofer) -->
          <div class="absolute floating-logo" style="top: 62%; left: 45%; animation-delay: 1.8s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('porsche-informatik')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500 bg-gray-600">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-5 sm:p-6 md:p-7 border border-white/40 shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2">
                <img 
                  src="/logos/porsche_informatik_logo.jpg" 
                  alt="Porsche Informatik" 
                  class="h-18 sm:h-20 md:h-22 lg:h-24 xl:h-26 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
                
                <!-- Hover Tooltip -->
                <div class="absolute -top-16 left-1/2 transform -translate-x-1/2 bg-white/90 backdrop-blur-lg text-gray-800 px-4 py-3 rounded-xl text-sm font-medium opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none whitespace-nowrap z-70 border border-white/40 shadow-lg">
                  <div class="text-center">
                    <div class="font-semibold text-gray-700">Porsche Informatik</div>
                    <div class="text-xs text-gray-600">Oct 2025 – Dec 2025</div>
                  </div>
                  <!-- Tooltip Arrow -->
                  <div class="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-white"></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Fraunhofer IEE - Upper Middle -->
          <div class="absolute floating-logo" style="top: 8%; left: 45%; animation-delay: 0s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('fraunhofer-iee')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-25 group-hover:opacity-40 transition-opacity duration-500"
                   style="background-color: #619E7E">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 sm:p-7 md:p-8 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #619E7E40; box-shadow: 0 10px 30px rgba(97, 158, 126, 0.15), 0 6px 15px rgba(97, 158, 126, 0.1)">
                <img 
                  src="/logos/fraunhofer-iee-logo.jpg" 
                  alt="Fraunhofer IEE" 
                  class="h-16 sm:h-18 md:h-20 lg:h-22 xl:h-24 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
                <!-- Active Badge -->
                <div class="absolute -top-2 -right-2 w-6 h-6 rounded-full flex items-center justify-center"
                     style="background-color: #619E7E">
                  <div class="w-2.5 h-2.5 bg-white rounded-full"></div>
                </div>
                
                <!-- Hover Tooltip -->
                <div class="absolute -top-8 left-1/2 transform -translate-x-1/2 bg-white/90 backdrop-blur-lg text-gray-800 px-4 py-3 rounded-xl text-sm font-medium opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none whitespace-nowrap z-40 border shadow-lg"
                     style="border-color: #619E7E40; box-shadow: 0 8px 25px rgba(97, 158, 126, 0.15);">
                  <div class="text-center">
                    <div class="font-semibold" style="color: #619E7E;">Fraunhofer IEE</div>
                    <div class="text-xs text-gray-600">Jul 2024 – Present</div>
                  </div>
                  <!-- Tooltip Arrow -->
                  <div class="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-white"></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Greenable GmbH - Middle Right (Aligned with BRAC IT) -->
          <div class="absolute floating-logo" style="top: 35%; right: 5%; animation-delay: 3.2s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('greenable')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500"
                   style="background-color: #56863F">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 sm:p-7 md:p-8 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #56863F40; box-shadow: 0 10px 30px rgba(86, 134, 63, 0.15), 0 6px 15px rgba(86, 134, 63, 0.1)">
                <img 
                  src="/logos/greenable-logo.jpg" 
                  alt="Greenable GmbH" 
                  class="h-16 sm:h-18 md:h-20 lg:h-22 xl:h-24 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
                
                <!-- Hover Tooltip -->
                <div class="absolute -top-16 left-1/2 transform -translate-x-1/2 bg-white/90 backdrop-blur-lg text-gray-800 px-4 py-3 rounded-xl text-sm font-medium opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none whitespace-nowrap z-60 border shadow-lg"
                     style="border-color: #56863F40; box-shadow: 0 8px 25px rgba(86, 134, 63, 0.15);">
                  <div class="text-center">
                    <div class="font-semibold" style="color: #56863F;">Greenable GmbH</div>
                    <div class="text-xs text-gray-600">Oct 2023 – Apr 2024</div>
                  </div>
                  <!-- Tooltip Arrow -->
                  <div class="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-white"></div>
                </div>
              </div>
            </div>
          </div>

          <!-- BRAC IT - Middle Left -->
          <div class="absolute floating-logo" style="top: 35%; left: 15%; animation-delay: 6.1s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('brac-it')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500"
                   style="background-color: #0A2D59">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 sm:p-7 md:p-8 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #0A2D5940; box-shadow: 0 10px 30px rgba(10, 45, 89, 0.15), 0 6px 15px rgba(10, 45, 89, 0.1)">
                <img 
                  src="/logos/brac-it-logo.jpg" 
                  alt="BRAC IT" 
                  class="h-16 sm:h-18 md:h-20 lg:h-22 xl:h-24 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
                
                <!-- Hover Tooltip -->
                <div class="absolute -top-16 left-1/2 transform -translate-x-1/2 bg-white/90 backdrop-blur-lg text-gray-800 px-4 py-3 rounded-xl text-sm font-medium opacity-0 group-hover:opacity-100 transition-all duration-300 pointer-events-none whitespace-nowrap z-60 border shadow-lg"
                     style="border-color: #0A2D5940; box-shadow: 0 8px 25px rgba(10, 45, 89, 0.15);">
                  <div class="text-center">
                    <div class="font-semibold" style="color: #0A2D59;">BRAC IT</div>
                    <div class="text-xs text-gray-600">Jul 2019 – Sep 2022</div>
                  </div>
                  <!-- Tooltip Arrow -->
                  <div class="absolute top-full left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-transparent border-t-white"></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Enhanced Flying Particles for Dynamic Depth -->
          <div class="absolute top-[15%] left-[38%] w-2 h-2 bg-blue-400/30 rounded-full floating-particle" style="animation-delay: 2.4s;"></div>
          <div class="absolute top-[32%] right-[45%] w-3 h-3 bg-green-400/25 rounded-full floating-particle" style="animation-delay: 4.7s;"></div>
          <div class="absolute top-[55%] left-[55%] w-2.5 h-2.5 bg-purple-400/20 rounded-full floating-particle" style="animation-delay: 7.3s;"></div>
          <div class="absolute top-[25%] left-[65%] w-1.5 h-1.5 bg-orange-400/25 rounded-full floating-particle" style="animation-delay: 1.8s;"></div>
          <div class="absolute top-[45%] right-[25%] w-2 h-2 bg-pink-400/20 rounded-full floating-particle" style="animation-delay: 5.5s;"></div>
          <div class="absolute top-[18%] left-[20%] w-1 h-1 bg-cyan-400/30 rounded-full floating-particle" style="animation-delay: 3.2s;"></div>
        </div>

        <!-- Mobile/Tablet: Grid Layout -->
        <div class="lg:hidden grid grid-cols-2 gap-10 p-8 h-full items-center justify-items-center">
          <!-- Fraunhofer IEE -->
          <div class="floating-logo mobile-logo" style="animation-delay: 0s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('fraunhofer-iee')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-25 group-hover:opacity-40 transition-opacity duration-500"
                   style="background-color: #619E7E">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #619E7E40; box-shadow: 0 10px 30px rgba(97, 158, 126, 0.15), 0 6px 15px rgba(97, 158, 126, 0.1)">
                <img 
                  src="/logos/fraunhofer-iee-logo.jpg" 
                  alt="Fraunhofer IEE" 
                  class="h-14 sm:h-16 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
                <!-- Active Badge -->
                <div class="absolute -top-2 -right-2 w-5 h-5 rounded-full flex items-center justify-center"
                     style="background-color: #619E7E">
                  <div class="w-2 h-2 bg-white rounded-full"></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Greenable GmbH -->
          <div class="floating-logo mobile-logo" style="animation-delay: 0.7s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('greenable')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500"
                   style="background-color: #56863F">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #56863F40; box-shadow: 0 10px 30px rgba(86, 134, 63, 0.15), 0 6px 15px rgba(86, 134, 63, 0.1)">
                <img 
                  src="/logos/greenable-logo.jpg" 
                  alt="Greenable GmbH" 
                  class="h-14 sm:h-16 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
              </div>
            </div>
          </div>

          <!-- BRAC IT -->
          <div class="floating-logo mobile-logo" style="animation-delay: 1.4s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('brac-it')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500"
                   style="background-color: #0A2D59">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-6 border shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2"
                   style="border-color: #0A2D5940; box-shadow: 0 10px 30px rgba(10, 45, 89, 0.15), 0 6px 15px rgba(10, 45, 89, 0.1)">
                <img 
                  src="/logos/brac-it-logo.jpg" 
                  alt="BRAC IT" 
                  class="h-14 sm:h-16 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
              </div>
            </div>
          </div>

          <!-- Porsche Informatik -->
          <div class="floating-logo mobile-logo" style="animation-delay: 2.1s;">
            <div class="group relative cursor-pointer" onclick="scrollToExperience('porsche-informatik')">
              <div class="absolute inset-0 rounded-3xl blur-lg opacity-20 group-hover:opacity-35 transition-opacity duration-500 bg-gray-600">
              </div>
              <div class="relative bg-white/70 backdrop-blur-lg rounded-3xl p-5 border border-white/40 shadow-xl hover:shadow-2xl transition-all duration-500 hover:scale-110 hover:-translate-y-2">
                <img 
                  src="/logos/porsche_informatik_logo.jpg" 
                  alt="Porsche Informatik" 
                  class="h-16 sm:h-18 w-auto object-contain transition-all duration-500"
                  loading="lazy"
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Clean Professional Company Stats (Simplified) -->
  <div class="absolute bottom-0 left-0 right-0">
    <div class="relative overflow-hidden">
      <!-- Subtle Background Glow -->
      <div class="absolute inset-0 h-20"
           style={`background: linear-gradient(180deg, transparent 0%, ${siteConfig.accentColor}08 50%, ${siteConfig.accentColor}15 100%)`}>
      </div>
      
      <!-- Elite Stats Dashboard -->
      <div class="relative z-10 px-8 sm:px-12 md:px-24 py-8">
        <div class="flex justify-center">
          <div class="flex items-center gap-6 sm:gap-8 px-6 sm:px-8 py-4 bg-white/20 backdrop-blur-2xl rounded-2xl border border-white/30 shadow-2xl hover:shadow-3xl transition-all duration-500 hover:scale-105 group">
            <div class="flex items-center gap-3">
              <div class="w-3 h-8 bg-gradient-to-t from-emerald-400 via-emerald-500 to-emerald-600 rounded-full shadow-lg group-hover:shadow-emerald-300/50 transition-shadow duration-300"></div>
              <span class="text-sm sm:text-base font-semibold text-gray-800 group-hover:text-gray-900 transition-colors duration-300">3 Global Companies</span>
            </div>
            <div class="w-px h-6 bg-gradient-to-b from-transparent via-gray-400/60 to-transparent"></div>
            <div class="flex items-center gap-3">
              <div class="w-3 h-8 bg-gradient-to-t from-blue-400 via-blue-500 to-blue-600 rounded-full shadow-lg group-hover:shadow-blue-300/50 transition-shadow duration-300"></div>
              <span class="text-sm sm:text-base font-semibold text-gray-800 group-hover:text-gray-900 transition-colors duration-300">4+ Years Excellence</span>
            </div>
            <div class="w-px h-6 bg-gradient-to-b from-transparent via-gray-400/60 to-transparent"></div>
            <div class="flex items-center gap-3">
              <div class="w-3 h-8 rounded-full shadow-lg transition-shadow duration-300 group-hover:shadow-purple-300/50" 
                   style="background: linear-gradient(to top, #8b5cf6, #a855f7, #c084fc)"></div>
              <span class="text-sm sm:text-base font-semibold text-gray-800 group-hover:text-gray-900 transition-colors duration-300">Tech Leadership</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* Dynamic flying animations - enhanced movement and rotation */
    @keyframes flyingLogoA {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) scale(1) rotate(0deg); 
      }
      15% { 
        transform: translateY(-12px) translateX(8px) scale(1.05) rotate(2deg); 
      }
      30% { 
        transform: translateY(-18px) translateX(-6px) scale(0.95) rotate(-1.5deg); 
      }
      45% { 
        transform: translateY(-8px) translateX(12px) scale(1.08) rotate(3deg); 
      }
      60% { 
        transform: translateY(-22px) translateX(-4px) scale(0.92) rotate(-2deg); 
      }
      75% { 
        transform: translateY(-6px) translateX(10px) scale(1.06) rotate(1.8deg); 
      }
    }

    @keyframes flyingLogoB {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) scale(1) rotate(0deg); 
      }
      20% { 
        transform: translateY(15px) translateX(-10px) scale(1.08) rotate(-2.5deg); 
      }
      35% { 
        transform: translateY(-8px) translateX(14px) scale(0.96) rotate(1.8deg); 
      }
      50% { 
        transform: translateY(20px) translateX(-7px) scale(1.04) rotate(-3deg); 
      }
      65% { 
        transform: translateY(-15px) translateX(11px) scale(0.94) rotate(2.2deg); 
      }
      80% { 
        transform: translateY(8px) translateX(-12px) scale(1.07) rotate(-1.5deg); 
      }
    }

    @keyframes flyingLogoC {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) scale(1) rotate(0deg); 
      }
      25% { 
        transform: translateY(-16px) translateX(-11px) scale(1.09) rotate(2.8deg); 
      }
      40% { 
        transform: translateY(-5px) translateX(9px) scale(0.97) rotate(-1.2deg); 
      }
      55% { 
        transform: translateY(-25px) translateX(-8px) scale(1.06) rotate(3.2deg); 
      }
      70% { 
        transform: translateY(-12px) translateX(13px) scale(0.93) rotate(-2.5deg); 
      }
      85% { 
        transform: translateY(-3px) translateX(-6px) scale(1.04) rotate(1.6deg); 
      }
    }

    @keyframes flyingLogoD {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) scale(1) rotate(0deg); 
      }
      12% { 
        transform: translateY(-20px) translateX(7px) scale(1.07) rotate(-2.8deg); 
      }
      28% { 
        transform: translateY(-4px) translateX(-13px) scale(0.95) rotate(2.2deg); 
      }
      44% { 
        transform: translateY(-17px) translateX(15px) scale(1.1) rotate(-1.8deg); 
      }
      60% { 
        transform: translateY(-28px) translateX(-5px) scale(0.91) rotate(3.5deg); 
      }
      76% { 
        transform: translateY(-9px) translateX(11px) scale(1.05) rotate(-2.2deg); 
      }
      88% { 
        transform: translateY(-2px) translateX(-8px) scale(0.98) rotate(1.4deg); 
      }
    }

    @keyframes floatParticleEnhanced {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) scale(1) rotate(0deg); 
        opacity: 0.2;
      }
      20% { 
        transform: translateY(-25px) translateX(18px) scale(1.4) rotate(120deg); 
        opacity: 0.6;
      }
      40% { 
        transform: translateY(-12px) translateX(-22px) scale(0.7) rotate(240deg); 
        opacity: 0.4;
      }
      60% { 
        transform: translateY(-35px) translateX(15px) scale(1.2) rotate(360deg); 
        opacity: 0.8;
      }
      80% { 
        transform: translateY(-8px) translateX(-18px) scale(0.9) rotate(480deg); 
        opacity: 0.3;
      }
    }

    .floating-logo {
      animation: flyingLogoA 8s ease-in-out infinite;
      transition: all 0.3s ease;
    }

    .floating-logo:nth-child(2) {
      animation: flyingLogoB 10s ease-in-out infinite;
    }

    .floating-logo:nth-child(3) {
      animation: flyingLogoC 7s ease-in-out infinite;
    }

    .floating-logo:nth-child(4) {
      animation: flyingLogoD 12s ease-in-out infinite;
    }

    .floating-particle {
      animation: floatParticleEnhanced 15s ease-in-out infinite;
    }

    /* Mobile responsive styling */
    .mobile-logo:nth-child(1) { animation: floatConstrainedA 10s ease-in-out infinite; animation-delay: 0s; }
    .mobile-logo:nth-child(2) { animation: floatConstrainedB 12s ease-in-out infinite; animation-delay: 0.7s; }
    .mobile-logo:nth-child(3) { animation: floatConstrainedC 8s ease-in-out infinite; animation-delay: 1.4s; }
    .mobile-logo:nth-child(4) { animation: floatConstrainedD 11s ease-in-out infinite; animation-delay: 2.1s; }
  </style>
</div>

<script>
  function scrollToExperience(companyId: string) {
    if (companyId === 'porsche-informatik') {
      // Scroll to achievements section for Porsche
      document.getElementById('achievements')?.scrollIntoView({ 
        behavior: 'smooth',
        block: 'start'
      });
      return;
    }

    // Scroll to experience section for other companies
    document.getElementById('experience')?.scrollIntoView({ 
      behavior: 'smooth',
      block: 'start'
    });

    // Wait for scroll to complete, then highlight the company
    setTimeout(() => {
      // Remove any existing highlights by resetting styles
      document.querySelectorAll('#experience .bg-white').forEach(el => {
        const htmlEl = el as HTMLElement;
        htmlEl.style.removeProperty('box-shadow');
        htmlEl.style.removeProperty('border-color');
        htmlEl.style.removeProperty('transform');
        htmlEl.style.removeProperty('transition');
      });

      // Find and highlight the specific company experience card
      const experienceCards = document.querySelectorAll('#experience .bg-white');
      const companyNames: Record<string, string> = {
        'fraunhofer-iee': 'Fraunhofer IEE',
        'greenable': 'Greenable GmbH',
        'brac-it': 'Brac IT',
        'porsche-informatik': 'Porsche Informatik'
      };

      // Company-specific highlight colors
      const highlightColors: Record<string, { color: string, rgba: string }> = {
        'fraunhofer-iee': { color: '#619E7E', rgba: '97, 158, 126' },
        'greenable': { color: '#56863F', rgba: '86, 134, 63' },
        'brac-it': { color: '#0A2D59', rgba: '10, 45, 89' },
        'porsche-informatik': { color: '#094882', rgba: '9, 72, 130' }
      };

      experienceCards.forEach(card => {
        // Look for the company name in the h3 element
        const companyElement = card.querySelector('h3');
        if (companyElement && companyElement.textContent?.trim() === companyNames[companyId]) {
          const highlightColor = highlightColors[companyId];
          const htmlCard = card as HTMLElement;
          
          // Apply company-specific highlight styling with !important to override
          htmlCard.style.cssText += `
            box-shadow: 0 0 0 4px rgba(${highlightColor.rgba}, 0.4), 0 0 25px rgba(${highlightColor.rgba}, 0.3), 0 15px 35px rgba(${highlightColor.rgba}, 0.15) !important;
            border-color: ${highlightColor.color} !important;
            transform: scale(1.03) !important;
            transition: all 0.3s ease !important;
          `;
          
          // Scroll the specific card into view for better positioning
          setTimeout(() => {
            card.scrollIntoView({ 
              behavior: 'smooth',
              block: 'center'
            });
          }, 200);
          
          // Remove highlight after 4 seconds
          setTimeout(() => {
            htmlCard.style.removeProperty('box-shadow');
            htmlCard.style.removeProperty('border-color');
            htmlCard.style.removeProperty('transform');
            htmlCard.style.removeProperty('transition');
          }, 4000);
        }
      });
    }, 800);
  }

  // Make function globally available
  (window as any).scrollToExperience = scrollToExperience;
</script>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }

  .animation-delay-800 {
    animation-delay: 0.8s;
  }

  /* Global styles for experience highlighting */
  :global(.experience-highlight) {
    animation: highlightPulse 0.6s ease-in-out;
    box-shadow: 0 0 0 3px var(--accent-color, #3b82f6), 0 10px 25px rgba(0, 0, 0, 0.15) !important;
    transform: scale(1.02);
    transition: all 0.3s ease;
  }

  @keyframes highlightPulse {
    0% { 
      box-shadow: 0 0 0 0 var(--accent-color, #3b82f6), 0 4px 6px rgba(0, 0, 0, 0.1);
      transform: scale(1);
    }
    50% { 
      box-shadow: 0 0 0 6px var(--accent-color, #3b82f6), 0 15px 30px rgba(0, 0, 0, 0.2);
      transform: scale(1.03);
    }
    100% { 
      box-shadow: 0 0 0 3px var(--accent-color, #3b82f6), 0 10px 25px rgba(0, 0, 0, 0.15);
      transform: scale(1.02);
    }
  }
</style>
